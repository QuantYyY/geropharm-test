import React, { FC } from "react";
import './Header.scss';

const Header: FC = () => {

    return(
        <>
            <div className="header">
            </div>
        </>
    );
}

export default Header;